# v0.1.4

- 主界面改为“完整群组目录 + 少量已选工作群”的聚焦工作区。
- 新增可搜索的“选择群组”窗口，并在本机记住工作群选择。
- “当前未读”固定为刷新群组目录时的未读快照，避免导出过程中后来消息混入。
- 采用方案 B：每个群独立提供“导出后标已读”开关，默认关闭，仅当前未读模式可用。
- 只有 result.json 成功写入后才会发送已读确认；导出失败绝不改变 Telegram 已读状态。
- 已读确认失败会独立报告，不会删除或否定已经成功生成的 JSON。
- 保留 v0.1.3 的 qasync 安全登录与非嵌套对话框修复。
